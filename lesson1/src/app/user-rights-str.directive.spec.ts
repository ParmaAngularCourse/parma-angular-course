import { UserRightsStrDirective } from './user-rights-str.directive';

describe('UserRightsStrDirective', () => {
  it('should create an instance', () => {
    const directive = new UserRightsStrDirective();
    expect(directive).toBeTruthy();
  });
});
