import { NewsTypeStylesDirective } from './news-type-styles.directive';

describe('NewsTypeStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsTypeStylesDirective();
    expect(directive).toBeTruthy();
  });
});
