import { ThemePipePipe } from './theme-pipe.pipe';

describe('ThemePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ThemePipePipe();
    expect(pipe).toBeTruthy();
  });
});
