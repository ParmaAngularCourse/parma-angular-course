import { NewsHeaderPipePipe } from './news-header-pipe.pipe';

describe('NewsHeaderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NewsHeaderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
