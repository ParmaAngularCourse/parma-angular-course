import { AdThemeDirective } from './ad-theme.directive';

describe('AdThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new AdThemeDirective();
    expect(directive).toBeTruthy();
  });
});
