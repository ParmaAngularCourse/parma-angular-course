import { SdRightsToChangeDirective } from './sd-rights-to-change.directive';

describe('SdRightsToChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new SdRightsToChangeDirective();
    expect(directive).toBeTruthy();
  });
});
