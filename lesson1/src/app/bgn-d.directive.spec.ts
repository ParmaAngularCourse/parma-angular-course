import { BgnDDirective } from './bgn-d.directive';

describe('BgnDDirective', () => {
  it('should create an instance', () => {
    const directive = new BgnDDirective();
    expect(directive).toBeTruthy();
  });
});
