import { RightsDDirective } from './rights-d.directive';

describe('RightsDDirective', () => {
  it('should create an instance', () => {
    const directive = new RightsDDirective();
    expect(directive).toBeTruthy();
  });
});
