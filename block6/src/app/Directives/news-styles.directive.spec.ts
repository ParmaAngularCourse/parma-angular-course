import { NewsStylesDirective } from './news-styles.directive';

describe('NewsStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsStylesDirective();
    expect(directive).toBeTruthy();
  });
});
