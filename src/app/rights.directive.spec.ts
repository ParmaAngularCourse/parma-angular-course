import { RightsDirective } from './rights.directive';

describe('RightsDirective', () => {
  it('should create an instance', () => {
    const directive = new RightsDirective();
    expect(directive).toBeTruthy();
  });
});
