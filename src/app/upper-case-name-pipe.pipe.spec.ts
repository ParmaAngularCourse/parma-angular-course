import { UpperCaseNamePipePipe } from './upper-case-name-pipe.pipe';

describe('UpperCaseNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaseNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
