import { FirstCapitalLetterPipe } from './first-capital-letter.pipe';

describe('FirstCapitalLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCapitalLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
