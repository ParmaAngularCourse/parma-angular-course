import { NewsTypeStyleDirective } from './news-type-style.directive';

describe('NewsTypeStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsTypeStyleDirective();
    expect(directive).toBeTruthy();
  });
});
