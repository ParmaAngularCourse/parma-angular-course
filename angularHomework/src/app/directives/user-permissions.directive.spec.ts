import { UserPermissionsDirective } from './user-permissions.directive';

describe('UserPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new UserPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
