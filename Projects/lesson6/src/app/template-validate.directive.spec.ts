import { TemplateValidateDirective } from './template-validate.directive';

describe('TemplateValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateValidateDirective();
    expect(directive).toBeTruthy();
  });
});
