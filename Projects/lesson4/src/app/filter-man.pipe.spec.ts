import { FilterManPipe } from './filter-man.pipe';

describe('FilterManPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterManPipe();
    expect(pipe).toBeTruthy();
  });
});
