import { StructDDirective } from './struct-d.directive';

describe('StructDDirective', () => {
  it('should create an instance', () => {
    const directive = new StructDDirective();
    expect(directive).toBeTruthy();
  });
});
