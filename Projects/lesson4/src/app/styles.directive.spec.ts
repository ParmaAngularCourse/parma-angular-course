import { StylesDirective } from './styles.directive';

describe('StylesDirective', () => {
  it('should create an instance', () => {
    const directive = new StylesDirective();
    expect(directive).toBeTruthy();
  });
});
