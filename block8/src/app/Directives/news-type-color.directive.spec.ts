import { NewsTypeColorDirective } from './news-type-color.directive';

describe('NewsTypeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsTypeColorDirective();
    expect(directive).toBeTruthy();
  });
});
