import { GetFirstLetterPipe } from './get-first-letter.pipe';

describe('GetFirstLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFirstLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
