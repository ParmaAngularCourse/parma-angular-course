import { GetNewsColorByTypeKeyPipe } from './get-news-color-by-type-key.pipe';

describe('GetNewsColorByTypeKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNewsColorByTypeKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
