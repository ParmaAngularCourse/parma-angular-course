import { GetNewsColorPipe } from './get-news-color.pipe';

describe('GetNewsColorPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNewsColorPipe();
    expect(pipe).toBeTruthy();
  });
});
