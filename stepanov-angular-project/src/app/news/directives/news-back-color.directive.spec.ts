import { NewsBackColorDirective } from './news-back-color.directive';

describe('NewsBackColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsBackColorDirective();
    expect(directive).toBeTruthy();
  });
});
