import { NewsUserRoleRestrictionDirective } from './news-user-role-restriction.directive';

describe('NewsUserRoleRestrictionDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsUserRoleRestrictionDirective();
    expect(directive).toBeTruthy();
  });
});
