import { NewsShowTypePipe } from './news-show-type.pipe';

describe('NewsShowTypePipe', () => {
  it('create an instance', () => {
    const pipe = new NewsShowTypePipe();
    expect(pipe).toBeTruthy();
  });
});
