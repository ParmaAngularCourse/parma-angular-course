import { NewsTitleCapitalLetterPipe } from './news-title-capital-letter.pipe';

describe('NewsTitleCapitalLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsTitleCapitalLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
